import { render, screen } from "@testing-library/react";
import App from "../components/App"; // Correct path to App.js

test("renders App with ShoppingList and allows category selection", () => {
  render(<App />);
  // Add your test code here
});
